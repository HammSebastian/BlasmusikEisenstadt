import { TranslationInstrumentPipe } from './translation-instrument-pipe';

describe('TranslationInstrumentPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslationInstrumentPipe();
    expect(pipe).toBeTruthy();
  });
});
