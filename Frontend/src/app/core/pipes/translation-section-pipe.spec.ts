import { TranslationSectionPipe } from './translation-section-pipe';

describe('TranslationSectionPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslationSectionPipe();
    expect(pipe).toBeTruthy();
  });
});
