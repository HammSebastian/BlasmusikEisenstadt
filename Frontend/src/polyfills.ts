// This file is intentionally blank for now.
// Add polyfills as needed for browser support.
// For example, for running Angular applications in older browsers:
// import 'zone.js/dist/zone';  // Included with Angular CLI.
// (window as any).__Zone_disable_requestAnimationFrame = true; // Zone JS Bug Workaround for
// (window as any).__Zone_disable_on_property = true; // Zone JS Bug Workaround for
// import 'zone.js/dist/zone-patch-electron'; // Zone JS Bug Workaround for Electron
// import 'zone.js/dist/zone-patch-rxjs'; // Zone JS Bug Workaround for RxJS
// import 'zone.js/dist/zone-patch-canvas'; // Zone JS Bug Workaround for Canvas
// import 'zone.js/dist/zone-patch-promise-test'; // Zone JS Bug Workaround for Promise Test
// import 'zone.js/dist/zone-patch-fetch'; // Zone JS Bug Workaround for Fetch
// import 'zone.js/dist/zone-patch-cordova'; // Zone JS Bug Workaround for Cordova
// import 'zone.js/dist/zone-patch-socket-io'; // Zone JS Bug Workaround for Socket IO
// import 'zone.js/dist/zone-patch-xhr'; // Zone JS Bug Workaround for XHR
// import 'zone.js/dist/zone-patch-webapis'; // Zone JS Bug Workaround for Web APIs
// import 'zone.js/dist/zone-patch-shadydom'; // Zone JS Bug Workaround for Shady DOM

// If you need to support older browsers that do not support certain ES6+ features,
// you might need to import core-js polyfills:
// import 'core-js/es/symbol';
// import 'core-js/es/object';
// ... and so on for other features.
// Make sure to install core-js: npm install core-js
import 'zone.js'; // Standard import for Zone.js
