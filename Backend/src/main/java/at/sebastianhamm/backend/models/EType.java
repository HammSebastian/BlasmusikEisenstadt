package at.sebastianhamm.backend.models;

/**
 * Enum representing different types/categories for announcements or messages.
 * Immutable set of standard categories.
 */
public enum EType {
    INFO,
    WARNING,
    MAINTENANCE,
    ANNOUNCEMENT
}
