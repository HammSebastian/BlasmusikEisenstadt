package at.sebastianhamm.backend.models.common.enums;

/**
 * Enum representing different general types or categories for announcements, messages, or content.
 * Provides a fixed set of standard classifications like informational, warning, etc.
 */
public enum EType {
    INFO,
    WARNING,
    MAINTENANCE,
    ANNOUNCEMENT
}
